You are a knowledge graph builder analyzing a debate session.

Given the speaker summaries and topics below, return ONLY a valid JSON object.
No markdown fences. No explanation. No text before or after the JSON.

JSON schema:
{{
  "nodes": [
    {{ "id": "unique_string", "label": "short name", "type": "speaker|topic|claim|consensus|shift", "weight": 1-10 }}
  ],
  "edges": [
    {{ "source": "node_id", "target": "node_id", "relation": "argues|opposes|supports|introduces|agrees_with|shifts_to|builds_on", "strength": 1-5, "label": "brief phrase" }}
  ]
}}

Rules:
- Every speaker MUST be a node with type "speaker"
- Every topic in the list MUST be a node with type "topic"
- Connect each speaker to topics they raised with relation "argues" or "introduces"
- When a speaker opposed another's point, create an edge with relation "opposes"
- If two speakers agreed on something, create a "consensus" node and connect both
- When discussion shifted from one topic to another, add a "shift" node between them with relation "shifts_to"
- Weight central topics 7-10, peripheral topics 1-4, speakers 6-8, claims 3-6
- Maximum {max_nodes} nodes total — be selective, drop least significant topics
- Edge strength 4-5 = strong relationship, 1-2 = weak or incidental

Speaker summaries:
{speakers_json}

Topics discussed:
{topics_list}
